import withRouter from '../../../components/Common/withRouter'

const LeadFileUploadTabView = ({ id }) => {
  return (
    <div>
      <h1>LeadFileUploadTabView</h1>
    </div>
  )
}

export default withRouter(LeadFileUploadTabView)
